# V1 + V2 Combined Benchmark Report (R67)

> Generated by building V1 from source and indexing the V2 codebase.
> Real data, real measurements — not synthetic.

## Methodology

1. **Built V1 from source**: `make -f Makefile.cbm cbm` (562 source files, 259MB binary)
2. **Indexed V2 codebase**: `cbm cli index_repository --repo-path v2/src --name bench-v2 --mode fast`
3. **Benchmarked V2 reading the same DB**: better-sqlite3 queries on `bench-v2.db`
4. **Benchmarked V1 CLI**: `cbm cli search_code` / `cbm cli list_projects`

## V1 Indexation Performance

| Metric | Value |
|---|---|
| Files indexed | 35 |
| Nodes extracted | 460 |
| Edges extracted | 1,499 |
| Functions analyzed (complexity) | 236 |
| Total time | **305ms** |
| Throughput | **~115 files/second** |
| Memory budget | 2,020 MB (auto-detected from 4 GB RAM) |

### V1 pipeline passes (timing breakdown)

| Pass | Time |
|---|---|
| configlink | 0ms |
| route_match | 0ms |
| complexity | 0ms |
| dump (gbuf → SQLite) | 5ms |
| persist_hashes | (included) |
| **Total pipeline** | **284ms** |

## V2 Query Performance (same DB, real data)

| Operation | Mean | Ops/sec | Assessment |
|---|---|---|---|
| getNodeById (prepared stmt) | 0.006ms | 182,845 | ✓ excellent |
| searchCode (LIKE %pattern%) | 0.077ms | 13,040 | ✓ excellent |
| countNodes (COUNT(*)) | 0.013ms | 74,481 | ✓ excellent |
| countAll (nodes + edges, 1 query) | 0.050ms | 20,207 | ✓ excellent |
| getBulkNodeDegrees (100 nodes) | 0.219ms | 4,566 | ✓ excellent |
| listNodes (200 results) | 1.195ms | 837 | ✓ good |

## V1 vs V2 — same SQLite DB

| Aspect | V1 (C API direct) | V2 (better-sqlite3) | Difference |
|---|---|---|---|
| SQLite query overhead | ~0.001ms | ~0.006ms | +0.005ms (JS binding) |
| CLI process startup | ~25ms per invocation | 0ms (already running) | V2 wins for interactive |
| Application-level cache | None | SWR (0.0003ms for hits) | V2 wins for repeated |
| Cypher query engine | Yes (query_graph) | No (fixed API only) | V1 wins for flexibility |

## V1 capabilities that V2 lacks

| Capability | V1 (C) | V2 (TypeScript) | Impact |
|---|---|---|---|
| **Code analysis** (tree-sitter, 158 languages) | ✅ 305ms for 35 files | ❌ Cannot do | V2 depends entirely on V1 |
| **Graph building** (nodes + edges from source) | ✅ | ❌ | V2 can only read, not create |
| **Complexity analysis** (cyclomatic) | ✅ | ❌ | V2 reads from properties_json |
| **Similarity detection** (simhash/minhash) | ✅ | ❌ | No code dedup in V2 |
| **Cross-repo intelligence** | ✅ | ❌ | No route matching across projects |
| **Git history analysis** | ✅ | ❌ | No file evolution tracking |
| **Trace ingestion** | ✅ | ❌ | No runtime path analysis |
| **Incremental indexing** | ✅ | ❌ | V2 can trigger re-index but can't do it |
| **Cypher query language** | ✅ query_graph | ❌ | V2 has fixed endpoints only |
| **Parallel pipeline** (12 workers) | ✅ | ❌ | V2 is single-threaded |
| **Arena/slab allocators** | ✅ | ❌ | V2 uses V8 GC |

## V2 capabilities that V1 lacks

| Capability | V2 (TypeScript) | V1 (C) | Impact |
|---|---|---|---|
| **Human memory graph** (ADRs, bugs, refactors) | ✅ | ❌ | V1 has no human context |
| **Obsidian vault sync** (Markdown) | ✅ | ❌ | V1 has no vault |
| **MCP server** (JSON-RPC for AI agents) | ✅ | ❌ | V1 has its own MCP but no human overlay |
| **React graph UI** (d3-force, WebSocket) | ✅ | ❌ | V1 has basic HTTP UI |
| **SWR cache** (stale-while-revalidate) | ✅ | ❌ | V1 re-queries every time |
| **Type safety** (TypeScript strict, 66 rounds) | ✅ | ❌ | V1 is C (weak types) |
| **WebSocket real-time** | ✅ | ❌ | V1 is request-response only |

## Verdict

V1 and V2 are **complementary, not competitive**:

- **V1** is the engine: it reads source code, understands 158 languages, builds the
  code graph, calculates complexity, detects similarity. Without V1, there is no
  code graph.
- **V2** is the interface: it serves the graph to AI agents (MCP), to humans (UI),
  and adds human context (ADRs, bugs, Obsidian sync). Without V2, the graph is
  just data in SQLite with no human overlay.

The benchmark confirms: V1 indexes in 305ms, V2 serves in 0.006ms. Together they
form a complete system. Neither is "better" — they are two halves of a whole.

## Caveat on node counts (R70 / Round 10 audit)

Node counts between V1 and V2 are **not directly comparable** as a measure of
extraction thoroughness. V2 (WASM, R69) counts each inline anonymous callback
(arrow functions, `.map(x => ...)`, `.then(...)`) as a separate Function node,
while V1 does not extract anonymous callbacks as standalone nodes. This means
V2's higher node count (784 vs 460) is partly a methodological difference in
what counts as an extractable unit, not necessarily a difference in completeness
or accuracy.
